<div class="container">
<h1>Lists</h1>

<% @lists.each do |list|  %>
<div class="card mb-5">
 <h2 class="card-title"><%= link_to list.name,  list_path(list)%></h2>
<% end %>
</div>
<%= link_to "Make new list",  new_list_path, class: "btn btn-primary"%>
</div>
